# 21 · 上游 PR 提交前复核（2026-09-18）

对象：`docs/upstream/proposal.patch`（6 提交 / 9 文件 / +376 −12），分支 `upstream/pr-b-adapter`
HEAD `e4fc454`，基线 `ea3ce05`。

## 结论

**代码与构建层面已达标，但对外文档与注释有 4 项 P1 必须先修。**
⇒ **不建议"此刻原样提交"；修完 P1 四项（均为文档/注释，不改逻辑）即可提交。**

> **2026-09-18 08:5x 更新**：4 项 P1 **已全部修复**（见 §六），并已按用户要求改用
> **基于 LakeUI 的完整上游播放器**重做验证（见 §七）。
> ⇒ **当前状态：可以提交。**

---

## 一、通过项（阻塞项全部通过）

| # | 项 | 结果 |
|---|---|---|
| 1 | 上游是否前进 | ✅ `ea3ce05..origin/master` **为空**（fetch 后确认），无需 rebase |
| 2 | 补丁可应用性 | ✅ 在干净上游导出树上 `git am --3way`，**6/6 全部应用，0 冲突** |
| 3 | 构建 | ✅ **0 error / 0 warning**（手工 cl/link/rc，22 步全 EXIT=0），产物 756,736 B |
| 4 | API 版本 | ✅ `FFF3FP_GetApiVersion()` = **15**；`PlayerApiVersion` / `VER_API` / 描述三处一致 |
| 5 | 本地专属内容泄漏 | ✅ `FFF3FP_Redraw` / `SetLogCallback` / `SetPresentConfig` / `SetPacingConfig` **全部为 0 命中** |
| 6 | 品牌 | ✅ `CompanyName` = **FFF_Project**（不是 3FCompare） |
| 7 | ABI | ✅ `preferredAdapterIndex` 追加在 `FFF3FPConfiguration` **末尾**；结构体大小 80 B |
| 8 | 实机功能 | ✅ 见下表 |
| 9 | 16F 修复（第 6 提交） | ✅ **首次实测覆盖**：PR 内核在 16 位路径 38 像素全一致 |
| 10 | 锁顺序 | ✅ 与既有 `ReadPixel` 一致（`deviceMutex_` → `presentMutex_`），未引入新并发路径 |
| 11 | 参数校验 | ✅ 见下 |

### 实机功能（PR 内核，本次重跑）

| 项 | 实测 |
|---|---|
| 4K 播放 | `video=3840x2160`，decoded/presented 递增 |
| A11 换卡 | `adapter=0` → vendor **4318**(NVIDIA)；`adapter=1` → vendor **32902**(Intel) ⇒ 真的换了卡 |
| A11 越界 | `99` / `-2` → `InvalidArgument` |
| `GetRenderTargetInfo` | swap/client/dest 均 1280×720 返回 Success |
| `ReadVideoPixelRegion` 10 位 | `outputBitDepth=10`，38 像素一致，**CONSISTENT** |
| `ReadVideoPixelRegion` 16 位（`FFF_TEST_HDR=1`） | `outputBitDepth=16`，38 像素一致，**CONSISTENT** |
| `SetViewTransform` | zoom 1.00/1.25/1.50/2.00 全 Success；`zoom=0` 与 `panX=nan` → `InvalidArgument`；之后仍存活 |

### 参数校验核查

- 回读：`dst==nullptr`、`width/height==0`、`dstFloatCount < w*h*4` → `InvalidArgument`；
  坐标 `x>=swapWidth_ || y>=swapHeight_` → `InvalidState`；非支持格式 → `NotSupported`；
  **`outputBitDepth` 有空指针保护**（`if (outputBitDepth != nullptr)`）。
- 变换：`!isfinite(zoom) || zoom<=0 || !isfinite(panX) || !isfinite(panY)` → `InvalidArgument`；
  disc 保护分支完整保留。
- 适配器：`preferredAdapterIndex < -1 || > 15` → `InvalidArgument`。

---

## 二、必须修的 P1（4 项）

### P1-1 · PR 描述已过期

`docs/upstream/proposal-description.md` 第 3 行仍写「5 个提交，9 文件 **+368 / -12**」，
English summary 第 132 行仍写「**Five** commits」。实际是 **6 提交 / +376 −12**。
⇒ maintainer 拿到的说明与补丁不符。

### P1-2 · 第 6 个提交在描述中完全未提及

第 6 个提交「修正 `ReadPixelRegion` 在 16F/HDR 下的越界读取与错误转换」**没有任何描述**。
该缺陷存在于本 PR 新增的批量回读代码里（`R16G16B16A16_FLOAT` 每通道是 HALF，
原按 float 读 ⇒ 数值全错 + 越界一倍），已由 A/B 实证并修复（`docs/19`）。
⇒ 必须在描述中补一句，说明"批量回读的 16 位分支已修正为按 HALF 逐通道转换"，
否则 maintainer 看不到这个修复，也无法判断是否需要同步修改调用方。

### P1-3 · 代码注释含内部编号与下游品牌（最终态仍存在）

补丁最终态中仍有以下注释（无后续提交清理）：

```
// 3FCompare patch (0004): batch pixel readback ...
// 3FCompare K1/K5: render-target diagnostics ...
// 3FCompare K4 diagnostics: record the drawn rect ...
// 3FCompare patch (0006 rev5): bypass the playback command queue ...
// 3FCompare extension (A11): preferred DXGI adapter index ...
// 3FCompare (A11) diagnostic: report which adapter actually backs the device.
```

`0004` / `K1/K5` / `K4` / `A11` 都是**我方内部编号**，对上游毫无意义，且暴露下游产品名。
⇒ 改为描述性英文注释（如 `// Batched pixel readback (single staging copy + Map).`）。
（`.rc` 的 `CompanyName` 与 `.gitignore` 注释**已经中性化**，这两处是好的。）

### P1-4 · 缺上游播放器侧适配说明

我们的 app 跑不了 PR 内核（缺本地专属导出），但**上游自己的 VB.NET 播放器**配 PR 内核需要
同步 3 处，否则 `FFF3FP_Create` 直接失败。这段对 maintainer 价值极高，应写进 PR：

1. `FFF.Player/Core/播放器会话.vb:41` 版本校验 `FFF3FP_GetApiVersion() <> 14UI` → **15UI**
2. 同文件 `:49` 配置 `.版本 = 14UI` → **15UI**
3. `FFF.Player/Interop/播放器原生接口.vb` 的 `原生播放器配置` 结构**末尾**追加
   `preferredAdapterIndex As Integer`，并建议初始化 `-1` 保持原行为
   （否则 `Marshal.SizeOf` 小于内核 `sizeof(config)`，`size >= sizeof(config)` 校验不过）

这也正好实证了描述里"必须同批次发布"的警告。

---

## 三、可选修的 P2（4 项）

- **P2-1** 提交信息含本机构建实测：「手工 cl/link/rc 编译 0 error」「`FileVersion=14.0.0.0`」
  「`CompanyName=FFF_Project`，版本资源正常生成」。按项目约定，对外不应写入本机实测内容。
- **P2-2** `.gitignore` 的**中间提交**仍含下游脚本引用 `tools/准备光盘库.ps1`
  （第 5 个提交已把最终态中性化，但按 commit 拆分合并时该历史会留下）
  ⇒ 建议把 `.gitignore` 相关的两个提交 squash。
- **P2-3** `dstFloatCount < width * height * 4u` 用**未裁剪**尺寸做 uint32 乘法，
  理论上可整数溢出。实际写入量被 `copyWidth/copyHeight`（按 swap 尺寸裁剪）限制，
  **不会越界写**；但建议改用 64 位或先限制 width/height 上限，让校验与实际写入一致。
- **P2-4** 每次调用都新建 staging 纹理（代码注释已诚实说明"调用方需节流"）。
  上游若没有节流调用方会有性能问题 ⇒ 建议在描述里点明这一点。

---

## 四、尚未解决但**不构成阻塞**的项

- **性能**：`--startup-regression` 在本地噪声压倒信号（同版本两次 GPU#3 得 0 与 61），
  **无证据表明 PR 引入退化，也无法证明没有**。属于测量工具在本环境失效，不是 PR 问题。
  若 maintainer 关心，可请其在稳定环境复测。
- **多路崩溃（issue #7）**：与本 PR 无关（PR 未触碰 Present/交换链竞态），
  且第三方 D3D 钩子（RTSS/ShadowPlay/MSI AB）未排除前不宜对外下结论。
- **PR 内核缺本地专属导出**：我们的 app 跑不了它是**已知且正常**的，不是缺陷。

---

## 五、建议的执行顺序

1. 改 `proposal-description.md`：补齐提交数、补 16F 修复说明、补上游播放器适配 3 处。（P1-1/2/4）
2. 中性化 `3FCompare` 内部编号注释，重新生成补丁并**重建确认 0 error/0 warning**。（P1-3）
3. 可选：squash `.gitignore` 两提交、清理提交信息中的本机构建实测。（P2）
4. 提交。

---

## 六、P1 修复情况（已完成）

| P1 | 处理 | 验证 |
|---|---|---|
| 1 描述过期 | `proposal-description.md` 改为 **7 提交 / +376 −12**；English summary 同步 | 已改 |
| 2 第 6 提交未提及 | 新增 §二.7「修正批量回读在 16 位交换链下的转换」+ English 第 7 条 | 已改 |
| 3 注释含内部编号/品牌 | 中性化 **20 处**（`3FCompare patch (0004)`/`K1/K5`/`K4`/`extension (A11)`/`patch (0006 rev5)`），首字母一并修正（13 行）；作为第 7 个提交提交，补丁重新生成 | 最终态 `grep 3FCompare` **0 命中**；新补丁在全新干净上游 `git am --3way` **7/7 应用**；**重建 0 error / 0 warning**，产物 756,736 B，API=15 |
| 4 缺播放器适配说明 | 新增 §三「上游播放器（`FFF.Player`）需要的同步修改」+ English 末尾说明 | 已改；且这 3 处**已实测跑通**（见 §七） |

顺带核实了描述中引用的结构体大小：**v14 = 72 字节 → v15 = 80 字节**（用 `sizeof` 实测，非推算）。

---

## 七、基于 LakeUI 完整上游播放器的验证（用户要求）

独立 C++ 程序只能证明内核 API 层，**真正的消费方是上游 VB.NET 播放器**。已按用户要求改用完整播放器验证。

### 环境

在 `.review_pr/player_env/` 搭了**完全隔离**的一套（不碰另一位 agent 正在用的共享播放器）：

```
player_env/LakeUI/LakeUI/bin/Debug/.../LakeUI.dll   ← 公开库，已构建
player_env/fff/FFF.Player                            ← git archive ea3ce05
player_env/fff/FFF.Player.Tests                      ← 上游自研控制台回归套件
player_env/fff/tools + third_party/vcpkg_installed    ← 构建所需（许可证资源 + 运行库）
```

依赖补齐踩到的坑：① 需 `tools/DiscRuntime.props` ② 需 `third_party/vcpkg_installed/x64-windows/share`（许可证嵌入式资源）③ 需其 `bin`（bluray/udfread/iconv/charset/libxml2）④ 内核要放到 `FFF.Native/x64/Release/` 才会被 `CopyNativeRuntime` 接受。

播放器侧 3 处适配由 `.review_pr/toggle_player.py`（幂等）在 PR/基线两态间切换。

### 结果（PR 内核 vs 原版 v14 内核，同一套播放器）

| 测试 | PR（v15） | 基线（v14） | 判定 |
|---|---|---|---|
| 完整播放器 + 测试套件构建 | **0 error / 0 warning** | 0 error / 0 warning | ✅ |
| `--startup-regression`（4K H.264） | **EXIT=0**，drop=**0**，present=240，first=**108.4ms** | EXIT=0，drop=**0**，present=240，first=**93.1ms** | ✅ 无退化 |
| `--hdr-switch-regression`（4K HDR10） | EXIT=1，超时 | EXIT=1，**同样超时** | ⚠️ 两版一致（归因见下方更正） |
| `--color-regression` | EXIT=1，MaxCLL 回退 1000 nit | EXIT=1，**完全相同** | ⚠️ 两版一致（归因见下方更正） |

> ⚠️ **2026-09-18 二次更正（见 `docs/22`）**：下方"重要更正 2"里
> **"本机未开启 Windows HDR"这一判断是错的，已撤回**。本机实测 `BitsPerColor=10`、
> `ColorSpace=RGB_FULL_G2084_NONE_P2020`（PQ），HDR **已开启且内核判据成立**。
> 两项失败的真正原因是：① `--hdr-switch-regression` 第二阶段依赖 VB 应用框架注入命令行
> 文件，在 `FFF.Player.Tests.exe` 入口下永不触发 ⇒ 会话从未创建；② `--color-regression`
> 硬编码期望 MaxCLL=1242 nit，本机素材为 1000 nit。**两者都与 HDR 能力、与 PR 无关。**

### 两条重要更正

1. **推翻"PR 版 drop 显著更高"的旧印象**：旧记录是 GPU drop 80/42/36（PR）vs 2/4/3（原版）。
   在这套**干净隔离**的完整播放器环境里，**两版都是 drop=0**，且都 EXIT=0。
   ⇒ 旧数字来自被并发任务（多路实机测试）打扰的环境，属噪声，不是 PR 退化。
2. ~~**HDR 相关两项失败是环境限制**~~ **（已撤回，见 `docs/22`）**
   原文称"本机未开启 Windows HDR，`OutputSupportsHdr()` 判据不成立"——**该判断错误**。
   实测本机 HDR 已开启（`BitsPerColor=10`、`ColorSpace=PQ`、`MaxLuminance=417.7 nits`），
   且 `--hdr-switch-regression` **第一阶段（真正的 HDR 切换验证）全部通过**。
   两项失败的真实原因是上游测试缺陷（路径注入失效）与素材常量不匹配（1242 vs 1000 nit），
   **与 HDR 能力无关，也与 PR 无关**。
